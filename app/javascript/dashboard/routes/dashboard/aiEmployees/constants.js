/**
 * Tab indices for AI Employee detail view (used by woot-tabs component)
 */
export const TAB_INDICES = {
  GENERAL: 0,
  WORKFLOWS: 1,
};

/**
 * Bot types (matching backend AgentBot enum)
 */
export const BOT_TYPES = {
  WEBHOOK: 'webhook',
  CSML: 'csml',
};
